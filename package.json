{
  "name": "ticketproject",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:phone": "turbo dev:phone",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "db:push": "turbo db:push --filter=@ticketproject/db",
    "reset": "pnpm --filter @ticketproject/server run wipe && pnpm --filter @ticketproject/db run db:push && pnpm --filter @ticketproject/server run seed:stops && pnpm --filter @ticketproject/server run seed && pnpm --filter @ticketproject/server run test:queries && pnpm --filter @ticketproject/server run test:trpc",
    "db:migrate": "turbo db:migrate --filter=@ticketproject/db",
    "db:studio": "turbo db:studio --filter=@ticketproject/db"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.0"
}
